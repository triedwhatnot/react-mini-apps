import { describe } from "vitest";


describe("initial render related tests", () => {
    it("should render an alert on inital render, giving relevant instructions", () => {

    });
    
    it("should render both sections on acknowledging alert", () => {
    
    });

    it("should display correct placeholder text in the textarea i.e. Enter your text here...", () => {

    });
});

describe('user interaction - happy flow', () => { 
    it("should empty the textbox on enter click", () => {

    });
    
    it("should relfect the textarea text in notepad section on enter click", () => {
    
    });
    
    it("should render multiple enteries of text in correct order in notepad section", () => {
    
    });

    it("should scroll down to last entry of notepad section after text is input and enter is pressed", () => {

    });

    it("should show a fading background color effect when text is rendered in notepad section", () => {

    });

});

describe("user interaction - edge cases", () => {
    it("shouldn't empty textarea value, if any other key is pressed other than enter key", () => {

    });

    it("shouldn't move textarea input to notepad section, if any other key is pressed other than enter key", () => {

    });
    
    it("should trim whitespaces before / after text entered in textarea when rendered in notepad section", () => {
    
    });

});
